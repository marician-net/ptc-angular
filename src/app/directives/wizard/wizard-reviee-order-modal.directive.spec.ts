import { WizardRevieeOrderModalDirective } from './wizard-reviee-order-modal.directive';

describe('WizardRevieeOrderModalDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardRevieeOrderModalDirective();
    expect(directive).toBeTruthy();
  });
});
