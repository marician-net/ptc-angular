import { WizardMobileConfirmDirective } from './wizard-mobile-confirm.directive';

describe('WizardMobileConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardMobileConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
