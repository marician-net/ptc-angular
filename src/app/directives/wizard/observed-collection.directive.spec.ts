import { ObservedCollectionDirective } from './observed-collection.directive';

describe('ObservedCollectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ObservedCollectionDirective();
    expect(directive).toBeTruthy();
  });
});
