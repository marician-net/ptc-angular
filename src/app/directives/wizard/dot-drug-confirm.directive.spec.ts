import { DotDrugConfirmDirective } from './dot-drug-confirm.directive';

describe('DotDrugConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new DotDrugConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
