import { WizardPrintformCheckboxDirective } from './wizard-printform-checkbox.directive';

describe('WizardPrintformCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardPrintformCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
