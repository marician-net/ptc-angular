import { WizardEmailConfirmDirective } from './wizard-email-confirm.directive';

describe('WizardEmailConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardEmailConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
