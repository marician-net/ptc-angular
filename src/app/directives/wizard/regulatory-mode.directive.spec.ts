import { RegulatoryModeDirective } from './regulatory-mode.directive';

describe('RegulatoryModeDirective', () => {
  it('should create an instance', () => {
      const directive = new RegulatoryModeDirective();
    expect(directive).toBeTruthy();
  });
});