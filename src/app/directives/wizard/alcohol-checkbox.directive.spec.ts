import { AlcoholCheckboxDirective } from './alcohol-checkbox.directive';

describe('AlcoholCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new AlcoholCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
